'use strict';

/**
 * A service used by applications using this module to render the Menu object as html
 */
class MenuService {

  /**
   * Constructor for the MenuService object
   * @param  {Menu} menu The instance of the Menu model to be used for rendering
   */
  constructor(menu){
    var menu = menu;
  };

  /**
   * Return the menu as an html element
   *
   * @return {?} An HTML representation of the menu
   */
  render(){
    // TODO
  }

}

module.exports = MenuService;
